# 光脑方案目录导引

> **版本**：v1.0  
> **更新日期**：2026年4月

本文档帮助您快速了解光脑方案项目的文件结构与各目录的用途。

## 项目根目录

```

light-brain/
├── README.md                  # 项目首页（中英双语）
├── LICENSE                    # MIT 许可证
├── .gitignore                 # Git 忽略规则
├── Cargo.toml                 # Rust 工作区配置
│
├── docs/                      # 设计文档（中英双语）
├── rust-core/                 # Rust 核心引擎
├── python-binding/            # PyO3 绑定层
├── python-ai/                 # Python AI 接口层
├── config/                    # 配置文件
├── data/                      # 运行时数据
├── scripts/                   # 脚本工具
├── tests/                     # 测试代码
├── experiments/               # 实验框架（四阶段目录预留）
├── extensions/                # 扩展模块
├── proto/                     # gRPC 接口定义（预留）
└── deployments/               # 部署配置

```

## 核心代码目录

### `rust-core/` —— Rust 核心引擎

光脑方案的符号推理、知识存储、生长调度和记忆管理均由 Rust 实现。该目录是一个独立的 Cargo 项目。

```

rust-core/
├── Cargo.toml                 # 核心库依赖配置
└── src/
├── lib.rs                 # 库入口，模块导出
├── utils/                 # 工具模块（时间、衰减、指标、ID生成）
├── thalamus/              # 丘脑模块（感知、接地、澄清）
├── prefrontal/            # 前额叶模块（调度、融合、工作记忆、DST、生长调度、生命之河）
├── amygdala/              # 杏仁核模块（风格推理、否决）
├── cerebellum/            # 小脑模块（语义记忆、三层流转、冲突检测）
├── hippocampus/           # 海马体模块（情景记忆、网状库存点、图扩散）
├── broca/                 # 布罗卡区模块（语言生成、护栏）
├── growth/                # 生长机制（Hebbian、神经元新生、知识抽取）
├── memory/                # 三层记忆管理
└── veto/                  # 否决机制

```

### `python-binding/` —— PyO3 绑定层

将 Rust 核心引擎的功能暴露为 Python 可调用的模块。

```

python-binding/
├── Cargo.toml                 # 绑定库依赖配置
└── src/
├── lib.rs                 # PyO3 模块入口，定义 Python 类
├── binding_helper.rs      # 绑定辅助函数
└── py_encoder.rs          # Python 编码器回调包装

```

### `python-ai/` —— Python AI 接口层

包含丘脑神经网络编码器和布罗卡区语言模型的 Python 实现。

```

python-ai/light_brain/
├── init.py                # 包入口
├── thalamus_encoder/          # 丘脑编码器（Transformer、原型匹配）
│   ├── encoder.py
│   ├── grounding.py
│   └── calibration.py
├── broca_slm/                 # 布罗卡区 SLM
│   ├── generator.py
│   ├── planner.py
│   ├── guardrails.py
│   └── constraint.py
└── utils/                     # Python 工具（配置、日志、设备）
├── config.py
├── logging.py
└── device.py

```

## 配置与数据目录

### `config/` —— 配置文件

| 文件 | 用途 |
|:---|:---|
| `prefrontal_decisions.json` | 前额叶决策表 |
| `amygdala_rules.json` | 杏仁核风格规则 |
| `broca_templates.json` | 布罗卡区兜底模板 |
| `creative_profiles.json` | 三旋钮模式预设参数 |
| `thalamus_prototypes.json` | 丘脑原型向量 |
| `guardrail_tiers.json` | 分级护栏词表 |
| `growth_config.json` | 生长配额配置 |
| `metacognition_config.json` | 元认知配置（阈值等） |
| `love_constraint.json` | 不可丢弃清单 |

### `data/` —— 运行时数据

| 路径 | 用途 |
|:---|:---|
| `knowledge/seed.json` | 种子知识库（最小自我说明书） |
| `hippocampus/` | 海马体 SQLite 数据库（运行时生成） |
| `models/thalamus_encoder/` | 丘脑编码器模型权重存放目录 |
| `models/broca_slm/` | 布罗卡区 SLM 模型权重存放目录 |
| `nirvana/` | 涅槃库（被扬弃的痕迹，运行时生成） |
| `dreams/` | 梦境沉积层（运行时生成） |

## 脚本与工具

### `scripts/` —— 脚本工具

| 脚本 | 用途 |
|:---|:---|
| `run.py` | 主入口，启动交互式对话 |
| `init_knowledge.py` | 初始化种子知识库，导入小脑 |
| `deploy_single.sh` | Linux/macOS 单机一键部署脚本 |
| `deploy_single.ps1` | Windows 单机一键部署脚本 |
| `download_models.py` | 模型下载指引脚本 |

## 文档目录

### `docs/` —— 设计文档

| 路径 | 内容 |
|:---|:---|
| `zh/完整架构说明.md` | 光脑方案架构、六模块、贯穿性机制、生命之河完整说明（中文） |
| `zh/实施路线图.md` | v1.0 / v2.0 / v3.0 版本规划与进度（中文） |
| `zh/贡献指南.md` | 代码贡献流程、风格规范、测试要求（中文） |
| `zh/种子库说明.md` | 种子知识库的设计原则与使用方式（中文） |
| `zh/模型下载指引.md` | 替换真实神经网络模型的指引（中文） |
| `zh/中英对照表.md` | 核心术语中英对照（中文） |
| `zh/目录导引.md` | 本文档（中文） |
| `en/` | 英文文档，结构与中文对应 |

## 测试与实验

### `tests/` —— 测试代码

```

tests/
├── rust_tests/                # Rust 单元测试与集成测试
└── python_tests/              # Python 端到端测试

```

### `experiments/` —— 实验框架

预留的四阶段实验目录，用于 v2.0 参数优化：
- `phase1_component/`：组件级验证
- `phase2_curve/`：单维度生长曲线拟合
- `phase3_synergy/`：神经-符号协同验证
- `phase4_stability/`：长期稳定性测试

## 扩展与部署

### `extensions/` —— 扩展模块

```

extensions/
└── public_kg/                 # 公库接入扩展（v3.0 完善）

```

### `proto/` —— gRPC 接口定义

预留的 Protocol Buffer 定义文件目录，用于 v3.0 分布式部署。

### `deployments/` —— 部署配置

```

deployments/
└── docker/                    # Docker 相关配置（Dockerfile、compose）

```

## 关键文件速查

| 想了解... | 查看文件 |
|:---|:---|
| 整体架构 | `docs/zh/完整架构说明.md` |
| 六模块协作 | `rust-core/src/prefrontal/decision_table.rs`、`fusion.rs` |
| 生长机制 | `rust-core/src/growth/scheduler.rs`、`neurogenesis.rs` |
| 否决与护栏 | `rust-core/src/amygdala/veto.rs`、`python-ai/light_brain/broca_slm/guardrails.py` |
| 生命之河自我模型 | `rust-core/src/prefrontal/` 下的 `circadian.rs`、`dream_task.rs`、`nirvana_task.rs`、`tao_miner.rs` 等 |
| Python 调用 Rust | `python-binding/src/lib.rs` |
| 主入口 | `scripts/run.py` |

---

*目录结构将随版本演进持续更新。*